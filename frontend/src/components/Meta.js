import React from 'react'
import { Helmet } from 'react-helmet'

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name='description' content={description} />
      <meta name='keyword' content={keywords} />
    </Helmet>
  )
}

Meta.defaultProps = {
  title: 'Welcome To AJIO - Fashion Store',
  description: 'We sell the best clothing for cheap',
  keywords: 'ecommerce, clothing, fashion',
}

export default Meta